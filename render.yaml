services:
  - type: web
    name: gif-app
    runtime: node
    buildCommand: cd server && npm install && cd .. && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
